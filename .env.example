# Google Apps Script Web App 的正式 /exec 部署網址。
# 請將「你的網址」替換為 Apps Script 部署後提供的實際網址。
# 本機使用時：複製本檔為 .env 後再填入值；請勿提交 .env。
# Vercel 使用時：請在 Project Settings > Environment Variables 設定相同變數。
VITE_GOOGLE_SHEETS_API_URL=https://script.google.com/macros/s/你的網址/exec
